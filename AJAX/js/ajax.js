//encapsulamos(closure) nuestro código ---> forma antigua de hacer modulos en js. Para evitar interferencias con otros codigos ,ya que utilizaremos nombres de variables iguales.
// xhr o ajax
(() => {
    const xhr = new XMLHttpRequest()
    
})()